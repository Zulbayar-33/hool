let adminRows=[],adminPassword=sessionStorage.getItem("akumaAdmin")||"";const $=id=>document.getElementById(id);const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));async function load(){$("refreshBtn").disabled=true;$("tableBox").innerHTML='<div class="notice info">Уншиж байна...</div>';try{const r=await Api.call("adminDashboard",{password:adminPassword});if(!r.success)throw new Error(r.message);$("adminLogin").classList.add("hidden");$("dashboard").classList.remove("hidden");adminRows=r.rows||[];$("fitStat").textContent=r.stats.fit;$("serviceStat").textContent=Number(r.stats.service||0).toLocaleString()+"₮";$("hoolStat").textContent=r.stats.hool;$("breakfastStat").textContent=r.stats.breakfast;$("breakfastMoneyStat").textContent=Number(r.stats.breakfastMoney||0).toLocaleString()+"₮";$("totalFoodStat").textContent=r.stats.totalFood;$("totalMoneyStat").textContent=Number(r.stats.totalMoney||0).toLocaleString()+"₮";$("pendingStat").textContent=r.stats.pending;render();$("logs").innerHTML=(r.logs||[]).map(x=>`<div class="log"><strong>${esc(x.action)}</strong><p>${esc(x.user)} • ${esc(x.detail)}</p><small class="muted">${esc(x.time)}</small></div>`).join("")||'<p class="muted">Лог байхгүй</p>'}catch(e){sessionStorage.removeItem("akumaAdmin");$("dashboard").classList.add("hidden");$("adminLogin").classList.remove("hidden");$("loginMsg").innerHTML=`<div class="notice error">${esc(e.message)}</div>`}finally{$("refreshBtn").disabled=false}}function render(){const f=$("sheetFilter").value,q=$("search").value.trim().toLowerCase();const rows=adminRows.filter(x=>(f==="all"||x.sheet===f)&&(!q||`${x.no} ${x.ovog} ${x.ner} ${x.job}`.toLowerCase().includes(q)));$("tableBox").innerHTML=`<table><thead><tr><th>Төрөл</th><th>№</th><th>Нэр</th><th>Албан тушаал</th><th>Огноо</th><th>Утга</th><th>Үйлдэл</th></tr></thead><tbody>${rows.map(x=>`<tr><td><span class="badge blue">${esc(x.sheet)}</span></td><td>${esc(x.no)}</td><td>${esc(x.ovog)} ${esc(x.ner)}</td><td>${esc(x.job)}</td><td>${esc(x.date)}</td><td><b>${esc(x.value)}</b></td><td>${x.pending?`<button class="btn btn-primary" onclick="pendingAction('${esc(x.sheet)}',${x.row},${x.col},'approve')">Зөвшөөрөх</button> <button class="btn btn-soft" onclick="pendingAction('${esc(x.sheet)}',${x.row},${x.col},'reject')">Татгалзах</button> `:``}<button class="btn btn-danger" onclick="removeEntry('${esc(x.sheet)}',${x.row},${x.col})">Устгах</button></td></tr>`).join("")||'<tr><td colspan="7">Мэдээлэл олдсонгүй</td></tr>'}</tbody></table>`}async function pendingAction(sheet,row,col,action){const r=await Api.call("adminAction",{password:adminPassword,sheet,row,col,action});if(!r.success){alert(r.message);return}load()}async function removeEntry(sheet,row,col){if(!confirm("Энэ бүртгэлийг устгах уу?"))return;const r=await Api.call("adminRemove",{password:adminPassword,sheet,row,col});if(!r.success){alert(r.message);return}load()}$("adminLoginBtn").onclick=()=>{adminPassword=$("adminPassword").value;sessionStorage.setItem("akumaAdmin",adminPassword);load()};$("refreshBtn").onclick=load;$("sheetFilter").onchange=render;$("search").oninput=render;if(adminPassword)load();
